/**
 * White Spaces...
 */
var xyz =    [  1, 2, 3,  4];

var a = function(b, c,  d) {
alert(b + c + d);
}
var c = a ? 10:20;
var bb = {"a":1,"b":2,"c":3};
var aa = (a + c) + 3;

for(var  i = 0, j = 0; i < 8 && j >= 0; i++, -- j) {
    alert ("(a " + a+ ",j " + j + ")");
}
if(xyz[1] >= 5) {
    switch (xyz[2]) {
        case 3 :
            return !(xyz[0] > 0);
            break;
        default :
            return xyz[0] > 2 ? 10  : 20;
    }
}
s =xyz[2];
c = new Array(a,b,c);
c.getCount(3);
c = ++s + arr[0]--;
void s;